@model IEnumerable<Demo.Project2.Models.Category>

<form class="d-flex ms-auto me-2" role="search">
    <div class="input-group my-3 my-lg-0">
        <select class="form-select border border-dark-subtle" aria-label="Default select example">
@foreach (var item in Model) {
        <optgroup label="@item.Name">
            @foreach (var childItem in item.ChildCategories.TakeWhile(b => b.IsActive == true)) {
                <option value="@childItem.Id">@childItem.Name</option>
            }
        </optgroup>
}
        </select>
        <input type="text" class="form-control border border-dark-subtle" placeholder="Tìm kiếm"
               aria-label="Tìm kiếm" aria-describedby="button-addon2">
        <button class="btn btn-light border border-dark-subtle" type="button" id="button-addon2">
            <i class="bi bi-search text-black"></i>
        </button>
    </div>
</form>